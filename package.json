{
  "name": "@agentplane/blueprints-catalog",
  "version": "0.1.0",
  "private": true,
  "description": "Catalog source for installable AgentPlane blueprints and lightweight packs.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-release.mjs",
    "check": "node scripts/validate-catalog.mjs && node scripts/build-release.mjs --check",
    "validate": "node scripts/validate-catalog.mjs",
    "sign:index": "node scripts/sign-index.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
