# Performance Benchmark Policy

Use this route only when a task outcome includes a performance claim.

Required evidence:

- benchmark command;
- environment and runtime version;
- warm/cold mode;
- repeat count;
- baseline value;
- comparison value;
- threshold;
- classification: faster, slower, unchanged, or noisy.

Stop if the method changes after implementation without approval.
